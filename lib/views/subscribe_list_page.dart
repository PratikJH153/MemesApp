import 'package:flutter/material.dart';

class SubsribeListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("SUBSCRIBER PAGE"),
    );
  }
}
